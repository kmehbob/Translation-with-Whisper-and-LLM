# Pinned exact versions.
# torch is intentionally NOT pinned to a plain PyPI wheel here: install it
# first from the CUDA-matched index for your GPU driver, e.g.
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# then install the rest of this file. See docs/AI_FEATURE.md and Dockerfile.
fastapi==0.115.6
uvicorn[standard]==0.34.0
transformers==4.47.1
accelerate==1.2.1
bitsandbytes==0.45.0
sentencepiece==0.2.0
protobuf==5.29.2
